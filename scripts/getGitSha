#!/bin/bash

echo "Getting Git SHA into ${1}"
if [[ ! -d "$(dirname "${1}")" ]]; then
  mkdir "$(dirname "${1}")"
fi
echo "git.sha = $(git rev-list -1 HEAD)" > "${1}"
echo "git.descriptor = $(git describe --dirty --always)" >> "${1}"